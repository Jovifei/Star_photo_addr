# 数据恢复、交互与原始八图复核

基线：772487dcb326d10140e82b19c23c88dfc9428d90。
执行边界：本地候选，未推送、未合并 main、未部署。未使用远端 GPT 网页。

## 本次实现

- 地图瓦片错误按 GridLayer 和失败 tile 记录；成功加载、瓦片移除、图层卸载均更新警告。成功的其他图层不会误清失败状态。提供重试，隔离 Leaflet 点击，避免误选点。
- 火烧云以逐日期 Promise.allSettled 处理；HTTP 失败只将对应旧快照标成 stale，保留地图/排行/详情一致的参考数据；恢复后同步解除降级。日期身份不符的响应拒收，切回缓存日期也刷新请求状态。
- 云海取消请求后不继续发重试，验证日期及 GFS 模型身份；乱序测试等待旧响应真正结束后再核对当前界面。
- ScoreRing 不再把 undefined/null/NaN/越界值当 0，显示 —；真实 0 分仍显示 0。
- 中文城市常驻标签按地图投影避让、去掉边缘裁切项，在平移、缩放、旋转时重新布局。只调整标签密度，不删除地点评分点。
- 数据源名称/状态改为三列对齐，降级不再使用绿色；声明文字不再静态声称天气/卫星始终可用。
- 点击采用 100–140ms 亮度/颜色反馈，不改变布局；尊重 reduced-motion。

## 原始截图复核（D05 证据阻塞解除）

本轮已从用户先前提供的 D:/Admin/Message/QQ Files/Tencent Files/977191131/nt_qq/nt_data/Pic/2026-09/Ori 目录成功读取全部 8 张原图。旧报告所称“需要用户再次提供”不再适用。以下是原图发现与当前候选的对应关系，不是生产/真机验收证明。

| 图 | 原文件名 | 发现与处置 |
|---|---|---|
| 1 | 9b81a78d0e2180fe6e77db6b44a01cf6.png | 顶部筛选过大、评分时刻省略、502 卡片盖地图。前序候选已折叠顶部、保留文档滚动；本轮补瓦片错误恢复。00:27 的评分日与前一观测夜允许不同，不能仅据日期不同改时间算法。 |
| 2 | 781b926b9d73a04506b2d194e58af956.png | 左侧仅露面板边缘、画布黑。当前候选已改固定底部面板与尺寸观察；网络失败独立提示，不能保证第三方瓦片永远可用。 |
| 3 | a4636c1d02888847a4fdd42996b42ee5.png | 非全宽面板，标题占用大，悬浮收起盖正文。前序候选统一面板关闭入口与锁滚动，现有面板/旋转测试覆盖。 |
| 4 | 7386d963f74a4dbdf02ceb89e6133dc9.png | 暂无数据却显示 0 星空分，本轮修成未知 —，补真实 0 与缺失值区别测试。原图海拔 0m 的来源仍需独立梳理（定位未给海拔时，计算兜底不能成为实测显示）。 |
| 5 | ff921cba296dcabfdd14f87de2cad8d2.png | B 档解释段被固定详情键遮挡；前序候选移除打开态浮动键，当前保留单一正文滚动区。目录参考不是现场 Bortle/SQM。 |
| 6 | 1e3596d7d217c4323cdf1b0a45cb2dad.png | 日期/周几被拆开，429、黑图、图例靠近浏览器工具栏。候选按需展开日期、图例回归文档；本轮增强请求身份校验和瓦片重试。真实 Safari 底部工具栏仍须真机验证。 |
| 7 | addce18e3c6f5d8fd8599c24c55657df.png | 南京/上海/杭州标签重叠、图例遮画布；图例布局已调整，本轮新增标签碰撞避让。分数色带表示条件指数，不能称实拍概率。 |
| 8 | a526b260c50786caf4f4e895eba9ce6c.png | 详情键遮云量卡片右侧，面板非全宽；候选已收敛入口。本轮视觉复核又找到数据源状态错列，已修三列布局与降级颜色。 |

## 验证证据纠正

- 原“Ctrl+= 连按三次”并未测量文字大小，不能证明 200%。已替换为 computed font-size 实测放大两倍的 CSS 文本压力测试；这是受控布局测试，不是操作系统字号或真实浏览器缩放资格。
- 原截图脚本中云海 390 文件名沿用了上一页的横屏视口，且 fullPage 会重现顶部而不能证明当前滚动位置；本轮重置每个专题的390视口、使用窗口截图，等待 scrollY 实际变化后采集。
- 整体应用截图仍含真实瓦片加载过程；不能把正在读取画面当成稳定数据已展示。数据恢复测试使用明确的故障注入，不能代表生产实时可用率。

## 本轮执行结果

- Node 24.18.0；npm run check：62文件 / 362项测试、lint、typecheck、生产构建通过。
- 最新212项Chromium全量：149 passed / 61 skipped / 2 failed。两项失败为数据源行用不同字体顶边而非中心判断对齐；仅改测试测量口径后，两项复跑全部通过。汇总151个执行用例通过，但这不是一次单轮151全绿记录。
- Firefox/WebKit：4 passed。本轮未把旧145项/352项成绩冒充当前结果。
- 火烧云成功→503→成功、地图瓦片重试不误选点、缺失评分、云海旧响应完成后不改新选择、两倍CSS文本与旋转恢复均有本次证据。
- 视觉重新检查390首页、下滑画面、火烧云日期展开态和云量面板；云量面板三列对齐已确认。截图在 tmp/mobile-content-flow-capture/。

## 尚未完成的项目边界（保留）

- 真机 Safari/Android 软键盘、浏览器底栏、后台切回与连续帧率：尚无本轮真机证据。
- 定位/搜索点海拔来源与近邻海拔估算口径需专项审计；不能从旧截图单独证明 0m 是实测。
- 第三方配额、瓦片服务、科学评分实拍校准仍依赖真实外部条件，不宣称由布局修复解决。
- 发布须经 Jovi 审核。当前为持续完善的本地候选，不把整个项目标成全部完成。
