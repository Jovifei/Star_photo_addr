# v1.0.6 production hotfix plan (2026-09-08)

## Goal and guardrails

- Base: `main@cee9dde09ed7a4d6e8736dc2ea020b244d49cd95`.
- Branch: `fix/v1.0.6-production-hotfix-20260908`.
- Goal: release `v1.0.6` with one package-backed version source, explicit unknown marker semantics for CloudSea/Fireglow, removal of the automatic DataV boundary fallback, and documentation/regression coverage.
- Prohibited: scoring weights, pressure-profile algorithms or thresholds, strict-majority rules, astronomy/Bortle/SQM strategy, location catalog, Phase 2 valley sampling, navigation, Planner/Finder restoration, and broad refactors.
- Production changes are allowed only after local gates and CI; rollback target is `cee9dde09ed7a4d6e8736dc2ea020b244d49cd95`.

## Execution checklist

1. **Root-cause/RED** — inspect version, marker, boundary and documentation paths; add focused failing tests before source edits. Evidence: each new regression fails for the intended missing behavior.
2. **Version source** — run `npm version 1.0.6 --no-git-tag-version`; add `src/lib/appVersion.ts`; wire `ProductHeader`, `healthz`, and `ChangelogModal` to the package-backed constants. Verify package and lock versions plus version tests. Failure: revert only hotfix edits; never alter the baseline.
3. **Unknown semantics** — extract or add pure marker classification helpers as needed; make null scores use muted unknown styling, popup/legend/detail `—/数据不足`, and keep valid p20 unchanged. Add CloudSea/Fireglow regression tests and verify overlay null exclusion remains intact. Failure: stop and restore the last green change; do not alter scoring.
4. **Boundary fallback** — remove only the automatic DataV remote request and its cache/effect; preserve local boundaries and `ChineseLabelLayer` Tianditu logic. Add a static regression for no DataV URL and retained Tianditu references. Failure: stop before deployment if any unintended boundary path changes.
5. **Docs and ledger** — align `README.md`, `docs/CHANGELOG.md` if applicable, `CHANGELOG.md`, and `tasks/todo.md` with the actual pressure-level contract, surface fields, unknown semantics, and Phase 2 boundary. Record the final review evidence and changed files.
6. **Local verification** — run `npm ci`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `npm run test:live`, then `npm run check`; run local E2E when the repository environment permits. Evidence: exit 0 and exact counts; any failure is reported before publishing.
7. **Publish/CI** — inspect `git diff --check` and the scoped diff, commit in focused commits, push the hotfix branch, create the PR, and wait for quality/live-data/container/Chromium/Firefox-WebKit checks. Merge only after 5/5 success.
8. **Production** — fast-forward local `main` to the merged SHA, build with `BUILD_REVISION` using the existing Compose files, preserve `.env`, Nginx and named volumes, and use only the documented temporary worker-stop/Swap procedure if needed. Verify containers, `/healthz`, `data-status`, `check:data-sources`, live smoke, four production routes, 375px layout, console/network, and absence of DataV requests.

## Stop conditions

- Stop immediately if `origin/main` changes before the branch is created, the worktree contains user changes, a forbidden algorithm/module is touched, any required local/CI gate is red, or production `healthz` is not `1.0.6` with the merged revision.
- Do not claim `PRODUCTION_HOTFIX_VALIDATION_PASS` while any required evidence is missing or any React/runtime/API/DataV error remains.
